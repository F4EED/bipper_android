# Synchronisation client web (alerte)

Modifications à reporter dans [client_web_MT_bipper](https://github.com/F4EED/client_web_MT_bipper) :

```text
integrations/client_web_MT_bipper/apps/web/...
  → apps/web/...   (même chemins relatifs)
```

Contenu : icône `bipper_alert_siren.svg` + navigation **Alertes** (remplace `BellIcon` / logo Gaulix pour cette entrée).
